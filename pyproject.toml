[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "bcrypt>=5.0.0",
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
    "supabase>=2.25.1",
    "werkzeug>=3.1.4",
]
